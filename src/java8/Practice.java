package java8;

public class Practice {

    public static void main(String[] args) {
        String str="ilovejavatechie";
        //count of characters
        /*
        Map<String, Long> strCount = Arrays.stream(str.split("")).collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));
        System.out.println(strCount);*/



    }
}
